
<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Body</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><%= @article.id %></td>
            <td><%= @article.name %></td>
            <td><%= @article.body %></td>
        </tr>
    </tbody>
</table>

MANUAL: 
<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Body</th>
        </tr>
    </thead>
    <tbody>
        <% @comments.each do |comment| %>
            <tr>
                <td><%= comment.id %></td>
                <td><%= comment.body %></td>
                <td><%= link_to 'Edit', edit_article_comment_path(@article.id, comment.id) %></td>
                <td><%= link_to 'Delete', article_comment_path(@article.id, comment.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>

            </tr>
        <% end %>
    </tbody>
</table>

PARTIAL: 
<%= render 'comments/index' %>
<%# <%=link_to 'edit comment', edit_article_comment_path(@article.id, @comment)%> %>

<%# removed 'comments: @comments' %>