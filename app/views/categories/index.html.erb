<table>
    <thead>
        <tr>
            <th>Category</th>
        </tr>
    </thead>

    <tbody>
        <% @categories.each do |category| %>
            <tr>
                <td><%= category.name %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<%= link_to 'New Category', new_category_path %>